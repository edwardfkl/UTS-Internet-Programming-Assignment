# Copy to .env.local and adjust if needed.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Use the same hostname as the shop (e.g. both 127.0.0.1 or both localhost). Needed for Sanctum/admin session and for the shared language cookie (storefront ↔ admin).
